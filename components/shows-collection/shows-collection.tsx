type ShowsCollectionProps = {
    shows: [];
    title: string;
}

export const ShowsCollection = ({ shows, title }: ShowsCollectionProps) => {
    return (
        <section>
            <span>MoviesCollection</span>
        </section>
    )
}